# Legacy Debug Scripts

Local `debug-*.mjs` scripts in this directory are intentionally ignored. They use the v1 `@jackwener/opencli` CDP bridge and are not part of the v2 package.

The maintained v1 reference implementation remains in `runtime/opencli-adapter.ts`.
